/*function validarFormulario() {
    var nome = document.getElementsByName("nome")[0].value;
    var sobrenome = document.getElementsByName("sobrenome")[0].value;
    var rg = document.getElementsByName("rg")[0].value;
    var telefone = document.getElementsByName("telefone")[0].value;
    var statusPassageiro = document.getElementsByName("statusPassageiro")[0].value;

    if (nome.trim() === '') {
        alert("Por favor, preencha o campo Nome.");
        return false; // Impede o envio do formulário
    }

    if (sobrenome.trim() === '') {
        alert("Por favor, preencha o campo Sobrenome.");
        return false; // Impede o envio do formulário
    }

    if (rg.trim() === '') {
        alert("Por favor, preencha o campo Registro Geral (RG).");
        return false; // Impede o envio do formulário
    }

    if (telefone.trim() === '') {
        alert("Por favor, preencha o campo Telefone.");
        return false; // Impede o envio do formulário
    }

    if (statusPassageiro.trim() === '') {
        alert("Por favor, selecione uma opção para a Preferência.");
        return false; // Impede o envio do formulário
    }

    return true; // Permite o envio do formulário
}*/
